# Terraform Validation & Fix Prompt

Tu es un senior Terraform engineer.

La validation terraform a échoué avec cette erreur:
{{error_message}}

Le code est dans: {{root_folder}}

Analyse les fichiers .tf et corrige les erreurs détectées.
